# 🚀 Деплой публичного бота - СЕЙЧАС

## Шаг 1: Создайте нового бота (2 минуты)

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте: `/newbot`
4. Имя: `HH Applicant Public Bot`
5. Username: `hh_applicant_public_bot` (или любой свободный)
6. **Скопируйте токен!**

## Шаг 2: Подготовка репозитория (3 минуты)

```bash
cd /Users/sus/hh-applicant-tool

# Коммитим новые файлы
git add public_bot.py auth_server.py Procfile requirements_public.txt
git commit -m "Add public bot"
git push
```

## Шаг 3: Деплой на Railway (5 минут)

### 3.1 Регистрация

1. Откройте [railway.app](https://railway.app)
2. **Login with GitHub**
3. Разрешите доступ

### 3.2 Создание проекта

1. **New Project**
2. **Deploy from GitHub repo**
3. Выберите `hh-applicant-tool`

### 3.3 Настройка переменных

В Railway → **Variables** добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### 3.4 Настройка процессов

Railway автоматически определит `Procfile` и запустит:
- `web` - auth_server (OAuth сервер)
- `worker` - public_bot (Telegram бот)

### 3.5 Получите URL

Railway даст URL типа: `https://hh-applicant-tool-production.up.railway.app`

**Скопируйте этот URL!**

## Шаг 4: Обновите код с вашим URL (2 минуты)

### 4.1 Обновите auth_server.py

Замените строку:
```python
REDIRECT_URI = "https://ваш-сервер.com/callback"
```

На:
```python
REDIRECT_URI = "https://ваш-railway-url.up.railway.app/callback"
```

### 4.2 Обновите public_bot.py

Замените строку:
```python
auth_url = f"https://ваш-сервер.com/auth?user_id={telegram_id}"
```

На:
```python
auth_url = f"https://ваш-railway-url.up.railway.app/auth?user_id={telegram_id}"
```

### 4.3 Закоммитьте изменения

```bash
git add public_bot.py auth_server.py
git commit -m "Update URLs"
git push
```

Railway автоматически перезапустится!

## Шаг 5: Тестирование (1 минута)

1. Откройте Telegram
2. Найдите вашего бота: `@hh_applicant_public_bot`
3. Отправьте `/start`
4. Нажмите кнопку "Авторизоваться"
5. Войдите на HH.RU
6. Вернитесь в бот
7. Отправьте `/letter Ваше сопроводительное письмо`
8. Отправьте `/apply`

## ✅ Готово!

Теперь **любой человек** может:
1. Найти бота в Telegram
2. Авторизоваться на HH.RU
3. Отправлять отклики

---

## 🔧 Альтернатива: Render.com

Если Railway не работает:

1. [render.com](https://render.com) → **New** → **Web Service**
2. Подключите GitHub
3. **Build Command:** `pip install -r requirements_public.txt`
4. **Start Command:** `gunicorn auth_server:app`
5. Добавьте переменную `TELEGRAM_BOT_TOKEN`

Для бота создайте отдельный **Background Worker**:
- **Build Command:** `pip install -e . && pip install -r requirements_public.txt`
- **Start Command:** `python public_bot.py`

---

## 📊 Мониторинг

### Логи Railway

```
Railway Dashboard → Deployments → Logs
```

### Проверка работы

```bash
# Проверка веб-сервера
curl https://ваш-url.railway.app/health

# Должен вернуть: {"status": "ok"}
```

---

## 🆘 Проблемы?

**Бот не отвечает:**
- Проверьте логи в Railway
- Убедитесь что `TELEGRAM_BOT_TOKEN` установлен
- Проверьте что оба процесса запущены (web + worker)

**Ошибка при авторизации:**
- Проверьте что URL в коде совпадает с Railway URL
- Убедитесь что используется HTTPS (Railway дает автоматически)

**База данных не создается:**
- Railway автоматически создаст `users.db` при первом запуске
- Проверьте права на запись в логах

---

## 🎉 Поздравляю!

Вы развернули публичного бота! Теперь делитесь ссылкой:

```
https://t.me/ваш_бот
```

И люди смогут автоматизировать отклики на HH.RU! 🚀
