services:
  - type: web
    name: hh-telegram-bot
    env: python
    buildCommand: pip install -e . && pip install python-telegram-bot==21.10
    startCommand: python telegram_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_USER_IDS
        sync: false
