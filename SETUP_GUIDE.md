# 🤖 Автоматизация откликов на HH.RU через Telegram бота

## Что это дает?

- ✅ Автоматическая рассылка откликов на вакансии (до 200/день)
- ✅ Управление через Telegram - отправляй отклики из любой точки мира
- ✅ Сохранение контактов работодателей в базу данных
- ✅ Автоматическое обновление резюме
- ✅ Статистика по откликам

---

## 📋 Пошаговая инструкция

### Шаг 1: Установка программы

#### Для Mac/Linux:

```bash
# Клонируем репозиторий
git clone https://github.com/s3rgeym/hh-applicant-tool
cd hh-applicant-tool

# Создаем виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Устанавливаем программу
pip install -e .

# Устанавливаем Telegram бот
pip install python-telegram-bot==21.10
```

#### Для Windows:

```powershell
# Клонируем репозиторий
git clone https://github.com/s3rgeym/hh-applicant-tool
cd hh-applicant-tool

# Создаем виртуальное окружение
python -m venv venv
.\venv\Scripts\activate

# Устанавливаем программу
pip install -e .

# Устанавливаем Telegram бот
pip install python-telegram-bot==21.10
```

---

### Шаг 2: Авторизация на HH.RU

```bash
# Активируем окружение (если еще не активировано)
source venv/bin/activate  # Mac/Linux
# или
.\venv\Scripts\activate   # Windows

# Авторизуемся
hh-applicant-tool auth

# Введите ваш email или телефон
# Введите код из SMS/Email
```

**Проверка:**
```bash
hh-applicant-tool whoami
```

Должно показать ваше имя и статистику.

---

### Шаг 3: Создание сопроводительного письма

Создайте файл `cover_letter.txt` в папке проекта:

```bash
nano cover_letter.txt  # или любой текстовый редактор
```

**Пример письма:**

```
Здравствуйте!

Меня заинтересовала ваша вакансия. Имею опыт работы в данной сфере и готов приступить к работе в ближайшее время.

Мои ключевые навыки:
- [Навык 1]
- [Навык 2]
- [Навык 3]

Готов обсудить детали сотрудничества.

С уважением,
[Ваше имя]
```

**⚠️ Важно:** Не используйте символы `%`, `→`, `—` - они могут вызвать ошибки.

---

### Шаг 4: Создание Telegram бота

#### 4.1. Создайте бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду: `/newbot`
4. Придумайте имя бота (например: "Мой HH Помощник")
5. Придумайте username (например: `my_hh_helper_bot`)
6. **Скопируйте токен** (выглядит так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### 4.2. Узнайте свой Telegram ID

1. Найдите [@userinfobot](https://t.me/userinfobot)
2. Отправьте `/start`
3. **Скопируйте ваш ID** (например: `123456789`)

---

### Шаг 5: Настройка бота

Скачайте файл бота в папку проекта:

```bash
# Файл telegram_bot.py уже должен быть в репозитории
# Если нет - создайте его (код ниже)
```

Создайте файл `.env` для хранения настроек:

```bash
nano .env
```

Добавьте туда:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_USER_IDS=ваш_telegram_id
```

**Пример:**
```
TELEGRAM_BOT_TOKEN=1885942942:AAGzEmy7tdiA8fl-YxwaU_PEIPk3rbMSHK0
TELEGRAM_USER_IDS=123456789
```

---

### Шаг 6: Запуск бота

#### Вариант 1: Простой запуск (для теста)

```bash
# Mac/Linux
export TELEGRAM_BOT_TOKEN="ваш_токен"
export TELEGRAM_USER_IDS="ваш_id"
source venv/bin/activate
python telegram_bot.py
```

```powershell
# Windows
$env:TELEGRAM_BOT_TOKEN="ваш_токен"
$env:TELEGRAM_USER_IDS="ваш_id"
.\venv\Scripts\activate
python telegram_bot.py
```

#### Вариант 2: Запуск в фоне (рекомендуется)

**Mac/Linux:**
```bash
chmod +x run_bot.sh
./run_bot.sh
```

**Windows:**
Создайте файл `run_bot.bat`:
```batch
@echo off
set TELEGRAM_BOT_TOKEN=ваш_токен
set TELEGRAM_USER_IDS=ваш_id
call venv\Scripts\activate
start /B python telegram_bot.py > bot.log 2>&1
echo Бот запущен!
```

Запустите:
```
run_bot.bat
```

---

### Шаг 7: Использование бота

1. Откройте Telegram
2. Найдите вашего бота (по username)
3. Отправьте `/start`

**Доступные команды:**

- `/apply` - Отправить отклики на вакансии
- `/update` - Обновить резюме (раз в 4 часа)
- `/whoami` - Информация об аккаунте
- `/stats` - Статистика откликов
- `/resumes` - Список резюме
- `/contacts` - Контакты работодателей
- `/help` - Справка

---

## 🔧 Настройка автозапуска

### Mac/Linux (systemd)

Создайте файл `/etc/systemd/system/hh-bot.service`:

```ini
[Unit]
Description=HH Applicant Tool Telegram Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/путь/к/hh-applicant-tool
Environment="TELEGRAM_BOT_TOKEN=ваш_токен"
Environment="TELEGRAM_USER_IDS=ваш_id"
ExecStart=/путь/к/hh-applicant-tool/venv/bin/python telegram_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl daemon-reload
sudo systemctl enable hh-bot
sudo systemctl start hh-bot
```

### Windows (Task Scheduler)

1. Откройте "Планировщик заданий"
2. Создайте задачу
3. Триггер: При входе в систему
4. Действие: Запустить `run_bot.bat`

---

## 📊 Мониторинг

### Просмотр логов

```bash
# Mac/Linux
tail -f bot.log

# Windows
type bot.log
```

### Проверка работы

```bash
# Mac/Linux
ps aux | grep telegram_bot

# Windows
tasklist | findstr python
```

### Остановка бота

```bash
# Mac/Linux
pkill -f telegram_bot.py

# Windows
taskkill /F /IM python.exe
```

---

## ❓ Частые вопросы

**Q: Бот не отвечает**
- Проверьте, запущен ли бот: `ps aux | grep telegram_bot`
- Проверьте логи: `tail -f bot.log`
- Проверьте токен бота

**Q: Ошибка при отправке откликов**
- Проверьте авторизацию: `hh-applicant-tool whoami`
- Проверьте сопроводительное письмо (нет спецсимволов)
- Возможно достигнут лимит (200/день)

**Q: Как добавить несколько пользователей?**
```
TELEGRAM_USER_IDS=123456789,987654321,555555555
```

**Q: Бот останавливается после перезагрузки**
- Настройте автозапуск (см. раздел выше)

---

## 🔒 Безопасность

- ✅ Никогда не публикуйте токен бота
- ✅ Обязательно укажите `TELEGRAM_USER_IDS`
- ✅ Храните `.env` в `.gitignore`
- ✅ Регулярно обновляйте программу

---

## 📦 Структура файлов

```
hh-applicant-tool/
├── venv/                    # Виртуальное окружение
├── telegram_bot.py          # Код бота
├── cover_letter.txt         # Сопроводительное письмо
├── run_bot.sh              # Скрипт запуска (Mac/Linux)
├── run_bot.bat             # Скрипт запуска (Windows)
├── .env                    # Настройки (НЕ ПУБЛИКОВАТЬ!)
├── bot.log                 # Логи бота
└── config/                 # Данные HH (токены, БД)
```

---

## 🚀 Готово!

Теперь вы можете управлять откликами на HH.RU прямо из Telegram!

**Полезные ссылки:**
- [Репозиторий проекта](https://github.com/s3rgeym/hh-applicant-tool)
- [Telegram канал](https://t.me/hh_applicant_tool)
- [Документация HH API](https://github.com/hhru/api)

---

## 💡 Советы

1. **Запускайте рассылку утром** - больше шансов, что HR увидит
2. **Обновляйте резюме каждые 4 часа** - будете выше в поиске
3. **Проверяйте контакты** - там могут быть прямые email/телефоны
4. **Не спамьте** - соблюдайте лимиты HH (200/день)
5. **Персонализируйте письмо** - укажите реальные навыки

Удачи в поиске работы! 🍀
